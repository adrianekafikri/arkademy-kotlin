<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arkademy.arkademytraining">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />

        <activity android:name=".week2.toolbar.ToolbarTestActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".week2.databinding.DataBindingTestActivity" />

        <activity android:name=".week3.LearnDialogActivity" />

        <activity android:name=".week3.LearnTabActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".week3.LearnSharedPreferenceActivity" />

        <activity android:name=".week3.LearnWebViewActivity" />

        <activity android:name=".week3.LearnRecyclerViewActivity" />

        <activity android:name=".week4.WordListActivity" />

        <activity android:name=".week4.AddWordActivity" />

        <activity android:name=".week4.LearnRetrofitActivity" />

        <activity android:name=".week5.MvpActivity" />

        <activity android:name=".week5.MvvmActivity" />
    </application>

</manifest>